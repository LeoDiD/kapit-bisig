# Server Environment Variables
# Copy this file to .env.local and fill in your values

# Server Configuration
PORT=3001
NODE_ENV=development

# MongoDB Connection
# Replace with your actual MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/kapit-bisig

# JWT Configuration
# IMPORTANT: Change this in production to a long, random string (at least 256 bits)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production-use-at-least-64-characters

# CORS Configuration
# In production, set this to your frontend domain
CORS_ORIGIN=http://localhost:3000

# Rate Limiting (optional overrides)
# LOGIN_RATE_LIMIT_WINDOW_MS=900000
# LOGIN_RATE_LIMIT_MAX=5
# REGISTRATION_RATE_LIMIT_WINDOW_MS=3600000
# REGISTRATION_RATE_LIMIT_MAX=3
